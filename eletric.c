#include <stdio.h>

int main(void)
{
	int n;
	int d, m, y, c;
	int i;

	while (1)
	{
		scanf ("%d", &n);
		if (!n) break;

		for (i = 0; i < n; i++)
		{
			scanf ("%d %d %d %d", d, m, y, c);
		}


	}
	return 0;
}

