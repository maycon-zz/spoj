#include <cstdio>
#include <cstdlib>
#include <cmath>


int main(void) {
	double x, y, r, p, q, a, t;

	while (scanf ("%lf %lf %lf", &x, &y, &r) && (x+y+r)) {
		scanf ("%lf %lf %lf", &p, &q, &a, &t);

	}

	return 0;
}

